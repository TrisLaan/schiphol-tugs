# Python
__pycache__/
*.pyc
.pytest_cache/
.coverage
.coverage.*
htmlcov/
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Editor/OS cruft
.DS_Store
Thumbs.db
.vscode/
.idea/

# Large, regeneratable visual-replay artefacts (see visualize.py --fresh).
# Figures and CSV/JSON metric outputs used by the report ARE tracked
# (results/*.png, results/*.csv except the ones below, results/*.json) so a
# reader can see them without re-running the pipeline.
results/animation*.mp4
results/animation*.gif
results/snapshots.csv
